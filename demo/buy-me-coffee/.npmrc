# Standalone app — use npm install here (not pnpm from repo root).
# @noderails/sdk is fetched from the public npm registry.
# Vercel: set project Root Directory to apps/buy-me-coffee (see vercel.json).
legacy-peer-deps=true
